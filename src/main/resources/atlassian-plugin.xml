<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>

    <!-- internationalize your plugin -->
    <resource type="i18n" name="com.heroku language" location="english"/>

    <!-- defines an example task -->
    <taskType name="bamboo" class="com.heroku.ExampleTask" key="test">
      <description>A simple Hello World Task</description>
      <!-- Categories available in 3.1: "builder", "test" and "deployment"
      <category name=""/>
      -->
      <configuration class="com.heroku.ExampleTaskConfigurator"/>
      <resource type="freemarker" name="edit" location="editExampleTask.ftl"/>
    </taskType>
</atlassian-plugin>
