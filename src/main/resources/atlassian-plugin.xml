<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}"/>
    </plugin-info>

    <!-- internationalize your plugin -->
    <resource type="i18n" name="com.heroku language" location="english"/>

    <!-- defines an example task -->
    <taskType key="com.heroku.ArtifactDeployment" name="Heroku: Deploy Artifact" class="com.heroku.ArtifactDeployment">
        <description>Deploy a build artifact to Heroku</description>
        <category name="deployment"/>
        <configuration class="com.heroku.ArtifactDeploymentConfigurator"/>
        <resource type="freemarker" name="edit" location="editArtifactDeployment.ftl"/>
        <resource type="download" name="icon" location="heroku_logo.png"/>
    </taskType>
</atlassian-plugin>
